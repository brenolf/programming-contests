#include <stdio.h>

int main(){
	int M[7][10], n = 0, i = 0, j = 0;
	int dig[10], maior[7];
	char e;
	
	while(1){
		scanf("%d", &n);
		
		if(!n)
			break;
			
		for(i = 0; i < 7; i++){
			maior[i] = -1;
			for(j = 0; j < 10; j++)
				M[i][j] = 0;
		}
		
		
		while(n--){
			for(i = 0; i < 10; i++){
				scanf("%d", &dig[i]);
				getchar();
			}
			
			for(i = 0; i < 7; i++){
				scanf("%c", &e);
				getchar();
				
				e -= 'A';
				
				M[i][dig[2 * e]]++;
				M[i][dig[2 * e + 1]]++;
				
				if(maior[i] < M[i][dig[2 * e]])
					maior[i] = dig[2 * e];
					
				if(maior[i] < M[i][dig[2 * e + 1]])
					maior[i] = dig[2 * e + 1];
			}
		}
		
		printf("-");
		
		for(i = 0; i < 7; i++)
			printf("%d ", maior[i]);
			
		printf("\n");
	}

	return 0;
}
