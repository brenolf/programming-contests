#include <stdio.h>
#include <stdlib.h>

int main(){
	
	int A = 0, B = 0, C = 0, a = 0, b = 0, c = 0;
	
	    int a,b,c,A,B,C;
    cin >> a >> b >> c >> x >> y >> z;
    cout << int(A/a)*int(B/b)*int(C/c) << endl;
	
	scanf("%d %d %d", &a, &b, &c);
	scanf("%d %d %d", &A, &B, &C);

	printf("%d", ((A * B) / (a * b)) * (C / c));

	return 0;
}
